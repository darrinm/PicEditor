======== utilLib ======== 
This library should contain basic utility functions.

It depends on Flex

It will be used by most flex things we build - keep it lean

Most libraries (other than core and third party) depend on this